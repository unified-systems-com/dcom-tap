# Test-collection marker for THIS project directory (NOT the plugin package). The
# plugin's importable code is the installed PEP 420 namespace package
# tap_plugin.dcom (tap_plugin/dcom/). This file exists only so pytest names a
# bare-checkout run's tests fully-qualified instead of colliding with another
# package-mode plugin's top-level `tests`. Ships in NO wheel.
